@page
@model SoccerGame.Pages.Teams.EditModel
@{
    ViewData["Title"] = "Edit";
}
<h2>Edit</h2>
<h4>Team</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Team.ID" />
            <div class="form-group">
                <label asp-for="Team.LastName" class="control-label"></label>
                <input asp-for="Team.LastName" class="form-control" />
                <span asp-validation-for="Team.LastName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Team.FirstMidName" class="control-label"></label>
                <input asp-for="Team.FirstMidName" class="form-control" />
                <span asp-validation-for="Team.FirstMidName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Team.HireDate" class="control-label"></label>
                <input asp-for="Team.HireDate" class="form-control" />
                <span asp-validation-for="Team.HireDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Team.GameAssignment.Location" class="control-label"></label>
                <input asp-for="Team.GameAssignment.Location" class="form-control" />
                <span asp-validation-for="Team.GameAssignment.Location" class="text-danger" />
            </div>
            <div class="form-group">
                <div class="table">
                    <table>
                        <tr>
                            @{
                                int cnt = 0;

                                foreach (var coach in Model.AssignedCoachDataList)
                                {
                                    if (cnt++ % 3 == 0)
                                    {
                                    @:</tr><tr>
                                    }
                                    @:<td>
                                        <input type="checkbox"
                                               name="selectedCoaches"
                                               value="@coach.CoachID"
                                               @(Html.Raw(coach.Assigned ? "checked=\"checked\"" : "")) />
                                        @coach.CoachID @:  @coach.Title
                                    @:</td>
                                }
                            @:</tr>
                            }
                        </table>
                    </div>
                </div>
                <div class="form-group">
                    <input type="submit" value="Save" class="btn btn-primary" />
                </div>
            </form>
        </div>
    </div>

    <div>
        <a asp-page="./Index">Back to List</a>
    </div>

    @section Scripts {
        @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    }